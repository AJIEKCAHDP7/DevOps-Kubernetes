DaemonSet — это контроллер в Kubernetes, который запускает по одному Pod’у на каждом узле (Node) кластера.

Проще говоря:
👉 Если в кластере 5 нод — будет 5 Pod’ов.
👉 Добавилась новая нода — Pod создастся автоматически.
👉 Удалилась нода — Pod удалится вместе с ней.

📌 Для чего используется DaemonSet

DaemonSet применяют для сервисов, которые должны работать на каждой ноде:

📊 Сбор логов (Fluentd, Filebeat)

📈 Мониторинг (Node Exporter)

🌐 Сетевые плагины

🔐 Службы безопасности

🧠 Как работает

DaemonSet:

Отслеживает количество нод в кластере

Гарантирует запуск одного Pod на каждой ноде

Может ограничиваться конкретными нодами через nodeSelector

🔎 ReplicaSet vs DaemonSet
	ReplicaSet	DaemonSet
Количество Pod	Указывается вручную	По одному на каждую ноду
Масштабирование	Через replicas	Автоматически по количеству нод
Основное назначение	Приложения	Системные сервисы

🎯 Главное отличие

ReplicaSet → управляет количеством реплик
DaemonSet → размещает Pod на каждой ноде
