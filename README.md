# DevOps-Kubernetes
All about K8s. Documentation, files, instructions &amp; etc....

K8s (Kubernetes) — это система для автоматического управления контейнерами (чаще всего Docker) в больших и распределённых приложениях.

Проще говоря: Kubernetes помогает запускать, масштабировать и поддерживать приложения в облаке или на серверах.

Зачем нужен Kubernetes

Когда много контейнеров, вручную управлять ими сложно. K8s умеет:

✅ Автоматически запускать приложения
✅ Перезапускать их при сбое
✅ Масштабировать нагрузку
✅ Распределять трафик
✅ Обновлять без простоя
✅ Экономить ресурсы

🔹 Основные понятия
📦 Pod
Минимальная единица — один или несколько контейнеров.

🖥 Node
Сервер в кластере.

🌐 Cluster
Группа серверов (Nodes).

⚙ Deployment
Управляет версиями и количеством Pod’ов.

🔌 Service
Даёт доступ к приложению по сети.

📈 Autoscaling
Автоматическое увеличение/уменьшение нагрузки.

---

🧩 1. Архитектура кластера

Кластер Kubernetes состоит из двух частей:

✅ Control Plane (управление)

👉 «Мозг» кластера — управляет всем

✅ Worker Nodes (рабочие ноды)

👉 «Рабочие руки» — запускают приложения

🧠 2. Control Plane — компоненты управления

Обычно работает на master-нодах.

🔹 2.1 API Server (kube-apiserver)

📌 Главный вход в кластер.

Все команды идут через него:

kubectl → API Server → cluster


Он:

принимает команды

проверяет права (RBAC)

валидирует данные

пишет в etcd

Без него кластер не работает.

🔹 2.2 etcd (база данных)

📌 Хранит всё состояние кластера.

Что хранит:

Pods
Nodes
Secrets
ConfigMaps
Users
Policies

Особенность:
распределённый
отказоустойчивый
работает по Raft

⚠️ Потеря etcd = потеря кластера.

🔹 2.3 Scheduler (kube-scheduler)

📌 Выбирает, на какой Node запускать Pod.

Учитывает:
CPU / RAM
метки (labels)
taints / tolerations
affinity

Пример:

«Этот Pod лучше пойдёт на Node2».

🔹 2.4 Controller Manager (kube-controller-manager)

📌 Следит за состоянием.

Запускает контроллеры:

Контроллер	Назначение
Deployment	Кол-во Pod
Node	Проверка Node
ReplicaSet	Реплики
Job	Задания
Namespace	Пространства

Он постоянно сравнивает:

«Как должно быть» vs «Как есть»

И исправляет.

🔹 2.5 Cloud Controller (в облаке)

📌 Интеграция с облаком.

Работает с:

AWS
Azure
GCP

Управляет:
LoadBalancer
дисками
IP

⚙️ 3. Worker Node — рабочие компоненты

На каждой рабочей ноде.

🔹 3.1 Kubelet

📌 Главный агент ноды.

Он:

получает команды от API Server

запускает Pod’ы

следит за ними

шлёт статус

Без kubelet Pod’ы не работают.

🔹 3.2 Container Runtime

📌 Запускает контейнеры.

Примеры:

containerd (чаще всего)

CRI-O

(Docker — устарел)

Он реально стартует контейнер.

🔹 3.3 Kube-proxy

📌 Сеть и балансировка.

Он:

настраивает iptables / ipvs

делает Service доступным

балансирует трафик

Пример:

Service → Pod1 / Pod2 / Pod3

🌐 4. Сеть в кластере (CNI)

Для сети используется CNI-плагин.

Популярные:

Плагин	Где
Calico	Часто
Flannel	Просто
Cilium	Современно
Weave	Старый

📌 Что делает CNI:

выдаёт IP Pod’ам

соединяет Pod’ы

даёт связь между нодами

Без CNI сеть не работает.

💾 5. Хранилище (Storage)
🔹 CSI (Container Storage Interface)

Подключает диски.

Поддержка:

AWS EBS

NFS

Ceph

Local

Объекты:

PV (Volume)

PVC (Claim)

StorageClass

🔐 6. Безопасность
🔹 RBAC

Права доступа.

Кто → что может.

🔹 Secrets

Хранит пароли / токены.

🔹 NetworkPolicy

Ограничение трафика.

📊 7. Мониторинг и логи (необязательно, но важно)

Часто ставят отдельно:

Инструмент	Назначение
Prometheus	Метрики
Grafana	Графики
Loki	Логи
EFK	Логи
🔄 8. Как всё работает вместе (поток)

Пример: создаём Pod.

kubectl apply
   ↓
API Server
   ↓
etcd (сохранил)
   ↓
Scheduler (выбрал Node)
   ↓
Kubelet (запустил)
   ↓
Runtime (создал контейнер)
   ↓
CNI (дал IP)
   ↓
kube-proxy (настроил сеть)

📌 9. Итоговая таблица
Компонент	Где	Задача
API Server	Control	Вход
etcd	Control	База
Scheduler	Control	Выбор Node
Controller	Control	Контроль
Kubelet	Worker	Запуск
Runtime	Worker	Контейнеры
Kube-proxy	Worker	Сеть
CNI	All	Подключение
CSI	All	Диски
🧠 В одном абзаце

Kubernetes — это система, где Control Plane принимает решения, etcd всё хранит, Scheduler выбирает сервер, Kubelet запускает контейнеры, CNI соединяет сеть, а kube-proxy даёт доступ.
