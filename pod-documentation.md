В Kubernetes у Pod есть несколько основных статусов (Phase), которые показывают, на каком этапе запуска он находится.

📌 Основные статусы Pod (Phase)

Команда для просмотра:
`kubectl get pods`


Пример вывода:

NAME        READY   STATUS     RESTARTS   AGE
my-pod      1/1     Running    0          5m


Поле STATUS — это статус Pod.

🔹 1️⃣ Pending (Ожидание)

📌 Pod создан, но ещё не запущен.

Причины:
- kube-scheduler не нашёл Node
- не хватает CPU / RAM
- нет Volume
- образ не скачан

Пример:
STATUS: Pending


👉 Проверка:

`kubectl describe pod my-pod`

🔹 2️⃣ Running (Работает)

📌 Pod запущен и работает.

- Все контейнеры стартовали
- Приложение доступно

Пример:
STATUS: Running

⚠️ Может быть Running, но READY 0/1 — значит контейнер ещё не готов.

🔹 3️⃣ Succeeded (Завершён успешно)

📌 Используется для Job.

Контейнер выполнился
Код выхода = 0

Пример:
STATUS: Succeeded

🔹 4️⃣ Failed (Ошибка)

📌 Pod завершился с ошибкой.

Причины:
crash
exit code ≠ 0
OOMKilled (не хватило памяти)

Пример:
STATUS: Failed

🔹 5️⃣ Unknown (Неизвестно)

📌 K8s не может получить статус.

Причина:
Node недоступна
проблемы с сетью
Редко, но бывает.

📌 Частые дополнительные состояния

Они видны в kubectl describe или kubectl get pod -o wide.

🔸 ContainerCreating

📌 Контейнер создаётся.

скачивается image

монтируются volume

настраивается сеть

Часто перед Running.

🔸 ImagePullBackOff / ErrImagePull

📌 Ошибка скачивания образа.

Причины:

неправильное имя image
нет доступа к registry
нет интернета

🔸 CrashLoopBackOff

📌 Контейнер падает и перезапускается.

Цикл:

Start → Crash → Restart → Crash → ...


Часто из-за ошибки в приложении.

🔸 Terminating

📌 Pod удаляется.

Может зависнуть, если есть проблемы с volume.

📌 Полный жизненный цикл Pod
Pending
   ↓
ContainerCreating
   ↓
Running
   ↓
Succeeded / Failed


или при ошибке:

Running → CrashLoopBackOff

📌 Как искать проблему

Если Pod не запускается:

1️⃣ Посмотри статус
`kubectl get pods`

2️⃣ Подробно
`kubectl describe pod my-pod`

3️⃣ Логи
`kubectl logs my-pod`

4️⃣ Если перезапускался
`kubectl logs my-pod --previous`

🧠 Коротко
Статус	Значение
Pending	Ждёт запуска
Running	Работает
Succeeded	Успешно завершён
Failed	Ошибка
Unknown	Нет связи
