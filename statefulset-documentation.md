StatefulSet — это контроллер в Kubernetes для запуска состояния-зависимых (stateful) приложений, таких как базы данных, кластеры и очереди сообщений.

📌 Зачем нужен StatefulSet

В отличие от Deployment:

✅ Каждый Pod имеет постоянное имя

✅ Каждый Pod получает свой Persistent Volume

✅ Поддерживается упорядоченный запуск и удаление

✅ Сохраняется стабильный сетевой идентификатор

🔎 Главное отличие от Deployment
	Deployment	StatefulSet
Имена Pod	Случайные	Постоянные (app-0, app-1)
Хранилище	Общее или временное	Отдельный PVC для каждого Pod
Запуск	Параллельный	Последовательный
Для БД	❌	✅

🔹 Как создаются Pod

Если replicas: 3, будут созданы:

nginx-statefulset-0

nginx-statefulset-1

nginx-statefulset-2

Каждый Pod:

Имеет свой PVC

Имеет стабильное DNS-имя

Запускается по порядку

⚠ Обязателен Headless Service

StatefulSet требует Service без ClusterIP:

clusterIP: None


Это нужно для стабильной сетевой идентификации.

🎯 Когда используют StatefulSet

📦 PostgreSQL

📊 MongoDB

📡 Kafka

🔴 Redis Cluster

📁 Приложения с постоянным хранилищем
