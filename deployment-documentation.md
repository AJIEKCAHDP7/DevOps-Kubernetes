Deployment — это контроллер в Kubernetes, который управляет созданием и обновлением Pod’ов через ReplicaSet.

Проще говоря:
👉 Deployment = управление версиями + масштабирование + автоматические обновления.

📌 Что умеет Deployment

✅ Создаёт ReplicaSet

✅ Управляет количеством реплик

✅ Делает Rolling Update

✅ Поддерживает Rollback

✅ Позволяет обновлять образ без остановки сервиса

🔎 Как работает Deployment

Создаёт ReplicaSet

ReplicaSet создаёт Pod’ы

При обновлении образа создаётся новый ReplicaSet

Старые Pod постепенно заменяются новыми

🆚 Deployment vs ReplicaSet
	Deployment	ReplicaSet
Rolling Update	✅	❌
Rollback	✅	❌
Управление версиями	✅	❌
Использование в production	✅	❌



RollingUpdate (по умолчанию)
🔹 Что делает

Постепенно заменяет старые Pod на новые без полной остановки сервиса.

🔹 Как работает

Создаётся новый ReplicaSet

Новые Pod запускаются постепенно

Старые Pod удаляются постепенно

Сервис остаётся доступным

🔹 Основные параметры
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 1   # Сколько Pod может быть недоступно
    maxSurge: 1         # Сколько дополнительных Pod можно создать временно

🔹 Пример

Если replicas = 3:

maxUnavailable: 1

maxSurge: 1

Процесс будет таким:

3 старых → 4 (1 новый добавлен)

4 → 3 (1 старый удалён)

Повторяется до полной замены

✅ Плюсы

Без простоя (Zero Downtime)

Подходит для production

Безопасное обновление

2️⃣ Recreate
🔹 Что делает

Полностью удаляет старые Pod и только потом создаёт новые.

🔹 Конфигурация
strategy:
  type: Recreate

🔹 Процесс

Все старые Pod удаляются

Затем создаются новые

⚠ Минусы

Есть простой (downtime)

Не подходит для высоконагруженных сервисов

✅ Когда используют

Когда нельзя запускать старую и новую версию одновременно

При конфликте с БД

При изменении схемы данных

📊 Сравнение стратегий
	RollingUpdate	Recreate
Downtime	❌ Нет	✅ Есть
Production	✅ Да	⚠ Иногда
Безопасность	Высокая	Средняя
Используется по умолчанию	✅ Да	❌ Нет
